<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sneaklife.pms.dao.system.authority.opera.OperaSbMapper">
  <select id="findOperaSbByShow" resultType="map" parameterType="map">
    select
      os.id as id,
      os.icon as icon,
      os.code as code,
      os.url as url,
      os.type as type,
      os.is_show as isShow,
      dd2.name as typeName,
      dd1.name as iconName,
      dd.name as codeName
    from opera_sb os
    inner join data_dictionary dd on os.code = dd.value
    inner join data_dictionary dd1 on os.icon = dd1.value
    inner join data_dictionary dd2 on os.type = dd2.value
    <where>
      os.is_del = 0
      <if test="isShow != null">
        AND os.is_show = #{isShow}
      </if>
      <if test="menuId != null and menuId != ''">
        AND os.menu_id = #{menuId}
      </if>
    </where>
  </select>
  <insert id="insertOperaSb" parameterType="com.sneaklife.pms.entity.OperaSb">
    insert into opera_sb(
      id,
      <if test="type != null and type != ''">
        type,
      </if>
      <if test="text != null and text != ''">
        text,
      </if>
      <if test="code != null and code != ''">
        code,
      </if>
      <if test="icon != null and icon != ''">
        icon,
      </if>
      <if test="url != null and url != ''">
        url,
      </if>
      <if test="menuId != null and menuId != ''">
        menu_id,
      </if>
      create_date,
      update_date,
      is_del,
      is_show
    )
    values(
      REPLACE(UUID(), '-', ''),
      <if test="type != null and type != ''">
        #{type,jdbcType=VARCHAR},
      </if>
      <if test="text != null and text != ''">
        #{text,jdbcType=VARCHAR},
      </if>
      <if test="code != null and code != ''">
        #{code,jdbcType=VARCHAR},
      </if>
      <if test="icon != null and icon != ''">
        #{icon,jdbcType=VARCHAR},
      </if>
      <if test="url != null and url != ''">
        #{url,jdbcType=VARCHAR},
      </if>
      <if test="menuId != null and menuId != ''">
        #{menuId,jdbcType=VARCHAR},
      </if>
      NOW(),
      NOW(),
      0,
      0
    )
  </insert>
  <select id="checkOperaSbByShow" parameterType="map" resultType="int">
    select count(id) from opera_sb where is_del = 0 AND is_show = 0 AND menu_id = #{menuId}
  </select>
  <update id="updateOperaSbShow" parameterType="map">
    update opera_sb
    <set>
      is_show = #{status}
    </set>
    <where>
      id = #{treeViewId}
    </where>
  </update>
  <select id="checkOperaSbById" parameterType="map" resultType="int">
    select count(id) from opera_sb where id = #{treeViewId} AND is_del = 0
  </select>
</mapper>
